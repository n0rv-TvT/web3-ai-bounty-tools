# Framework

Reusable framework scripts and helpers can live here.

Current imported Opencode Web3 scripts are preserved under:

```text
skills/web3-ai-bounty/scripts/
```

They remain inside the skill folder so relative skill documentation can refer to `scripts/`, `schemas/`, `examples/`, and `evals/` naturally.

Do not place generated outputs, benchmark results, private target data, secrets, or raw logs here.
