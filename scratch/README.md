# Scratch Directory

Public-safety warning: do not commit real private target data, unsanitized bounty reports, secrets, cookies, RPC URLs, raw logs, platform messages, or real user data here.

Use this directory for local temporary work only. It is ignored by default except for this README and `.gitkeep`.
